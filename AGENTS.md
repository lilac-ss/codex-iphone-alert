# 開発ルール

## 作業の基準

最初に `docs/PLAN.md`、`docs/DEVELOPMENT.md`、`docs/HANDOFF.md` を読む。
このリポジトリは独立した個人用ツールであり、別プロジェクトの業務要件や構成を持ち込まない。
説明、操作手順、引き継ぎは原則として日本語で記述する。

## 実装

- Mac用のNode.js補助ツールと、依存を抑えた静的PWAを実装する。
- Web Pushの暗号処理は既存ライブラリを利用し、依存バージョンとロックファイルを保存する。
- 秘密情報や端末登録情報を保持する場所はリポジトリ外とし、ディレクトリは所有者のみ、ファイルは所有者のみ読み書きできる権限を設定する。
- VAPID秘密鍵、購読endpoint・keys、実イベントpayload、個人設定、会話内容をログ、テストfixture、Git、Pagesに含めない。
- VAPID公開鍵は公開可能。秘密鍵と混同しない。
- Codexの既存notifyコマンド、hook、通知設定を保持し、導入・解除の手順を用意する。
- 承認要求の許可・拒否を返さず、承認方式とhookの信頼確認を変更・迂回しない。
- 外部コマンドには引数配列を使い、入力をシェルとして評価しない。
- 個人のCodex設定やLaunchAgentへの変更は、実装とテストの完了後の導入工程で行う。
- 公開するファイルを明示し、リポジトリ全体やMacの状態ディレクトリを配信しない。

## 検証と文書

- 時間とOS状態を差し替えられる設計にし、通知周期・期限・停止条件・重複処理・障害処理を自動テストする。
- テストは架空のデータを使用し、実端末・外部通信・秘密情報なしで実行できるようにする。
- コミット前に、その変更に関係するテストと検査を実行する。
- 実装時にpackage.jsonの実行コマンドを定義し、READMEと開発手順を実際のコマンドへ更新する。
- 実機で確認したこと、自動テストで確認したこと、未確認事項を区別して報告する。
- 設計上の重要な決定や変更理由はdocsに残す。ユーザーの確定要件を独断で変更しない。
